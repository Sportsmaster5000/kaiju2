/// @description Attempts to cast a spell at the given angle.

/*
//The caster object that is going to be attempted to be created.
var _spell = global.spells[spellIndex];
	
	
//Checks if the caster has enough SP to cast.
if (sp > _spell.cost) && (delayTimer <= 0)
{
	sp -= _spell.cost;
	delayTimer = _spell.delay;
		
	//Casts the spell at the given position.
	spell_cast(angle, _spell.caster);
}
*/